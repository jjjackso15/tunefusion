# packages/shared

Shared TypeScript types and utilities.

## Responsibilities (planned)
- Shared type definitions mirroring Rust structs (artifact kinds, DB DTOs)
- Zod schemas / runtime validation for JSON artifacts
- UI helpers for rendering artifacts

## Status
Scaffold only.
