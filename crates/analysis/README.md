# crates/analysis

Rust crate for TuneFusion's **artifact-first** analysis pipeline.

## Responsibilities (planned)
- Orchestrate analysis runs (pipeline versioning + caching)
- Produce versioned artifacts (pitch contour, chords, beat grid, etc.)
- Persist artifacts + metadata

## Status
Scaffold only.
